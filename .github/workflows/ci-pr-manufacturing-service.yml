name: PR Check — manufacturing-service

on:
  push:
    branches: ['feat-*', 'fix-*', 'chore-*']
    paths:
      - 'backend/manufacturing-service/**'
      - '.github/workflows/ci-pr-manufacturing-service.yml'
  pull_request:
    branches: [develop]
    paths:
      - 'backend/manufacturing-service/**'

permissions:
  contents: read
  pull-requests: read
  security-events: write
  actions: read

jobs:
  pr-check:
    uses: ./.github/workflows/_java-pr-check.yml
    with:
      service-name: manufacturing-service
      service-dir: backend/manufacturing-service
      needs-database: true
    secrets: inherit
