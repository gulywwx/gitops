name: PR Check — notification

on:
  push:
    branches: ['feat-*', 'fix-*', 'chore-*']
    paths:
      - 'backend/notification-service/**'
      - '.github/workflows/ci-pr-notification.yml'
  pull_request:
    branches: [develop]
    paths:
      - 'backend/notification-service/**'

permissions:
  contents: read
  pull-requests: read
  security-events: write
  actions: read

jobs:
  pr-check:
    uses: ./.github/workflows/_node-pr-check.yml
    with:
      service-name: notification-service
      service-dir: backend/notification-service
      node-version: '22'
    secrets: inherit
