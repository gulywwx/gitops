name: PR Check — qc-service

on:
  push:
    branches: ['feat-*', 'fix-*', 'chore-*']
    paths:
      - 'backend/qc-service/**'
      - '.github/workflows/ci-pr-qc-service.yml'
  pull_request:
    branches: [develop]
    paths:
      - 'backend/qc-service/**'

permissions:
  contents: read
  pull-requests: read
  security-events: write
  actions: read

jobs:
  pr-check:
    uses: ./.github/workflows/_java-pr-check.yml
    with:
      service-name: qc-service
      service-dir: backend/qc-service
      needs-database: false
    secrets: inherit